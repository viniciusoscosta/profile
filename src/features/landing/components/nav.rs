use leptos::prelude::*;

#[component]
pub fn Nav() -> impl IntoView {
    view! {

        <header class="h-20 w-screen flex justify-center">
            <nav class="h-20 w-[90%] bg-black"></nav>
        </header>
    }
}
